---
import Layout from "../../layouts/PageLayout.astro";
import { getLangFromUrl, useTranslations } from "../../i18n/utils";
import { ViewFullPost } from "../../components/posts/FullPostView";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const { param } = Astro.params;
---

<Layout>
    <div
      class="border border-border dark:border-border-DM rounded flex flex-col items-center w-96"
    >
      <h1 class="text-xl text-text1 dark:text-text1-DM py-4">
        {/* TODO: Add Page Title*/}
      </h1>
      <div class="w-full px-6 py-4">
        <ViewFullPost id={param} client:only="solid-js">
      </div>
    </div>
  </Layout>