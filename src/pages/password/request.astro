---
import { SITE } from "../../config";
import PageLayout from "../../layouts/PageLayout.astro";
import { Auth } from "../../lib/Auth";
import { NewPassword } from "../../components/users/RequestNewPassword";
import { getLangFromUrl, useTranslations } from "../../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<PageLayout pageTitle={t("pageTitles.requestPasswordReset")} pageDescription={t("pageDescriptions.requestPasswordReset")}>
  <div
    class="border border-border1 dark:border-border1-DM rounded flex flex-col items-center w-96"
  >
    <h2 class="text-xl text-ptext1 dark:text-ptext1-DM py-4">
      {t("pageTitles.requestPasswordReset")}
    </h2>
    <div class="w-full px-6 py-4">
      <NewPassword client:only="solid-js" />
    </div>
  </div>
</PageLayout>
