---
import { SITE } from "../../config";
import PageLayout from "../../layouts/PageLayout.astro";
import { Auth } from "../../lib/Auth";
import { NewPassword } from "../../components/users/RequestNewPassword";
import { getLangFromUrl, useTranslations } from "../../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<PageLayout>
  <div
    class="border border-border dark:border-border-DM rounded flex flex-col items-center w-96"
  >
    <h2 class="text-xl text-text1 dark:text-text1-DM py-4">
      {t("pageTitles.requestPasswordReset")}
    </h2>
    <div class="w-full px-6 py-4">
      <NewPassword client:only="solid-js" />
    </div>
  </div>
</PageLayout>
